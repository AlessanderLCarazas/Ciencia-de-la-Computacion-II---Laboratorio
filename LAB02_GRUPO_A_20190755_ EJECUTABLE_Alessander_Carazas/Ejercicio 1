#include <iostream>

using namespace std;

int main()
{
    int sum;
    for (int i=0;i<=100;i++)
    {
        if(i%2==0)
            sum+=i;
    }
    cout<<"La suma de los numeros enteros pares desde el 2 hasta en 100 es: "<<sum<<endl;
    return 0;
}
